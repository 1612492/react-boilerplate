import { FC } from 'react';
import { Box, Flex } from '@chakra-ui/react';

const promotion = ['10%', '20%', '30%'];
const sections = ['featured', 'lastest'];
const categories = ['Áo', 'Quần', 'Giày'];

const Body: FC = () => {
  return <Box></Box>;
};

export default Body;
